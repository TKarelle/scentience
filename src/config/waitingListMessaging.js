/**
 * Messages liste d’attente partagés (hero, sections, menu mobile).
 */
export const WAITING_LIST_STATUS = "— We are currently full —";

export const WAITING_LIST_CTA_LABEL = "JOIN WAITING LIST";
