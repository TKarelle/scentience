import { PRE_ORDER_CTA_LABEL } from "./preOrderMessaging";

/** Section journey — carte wine, pitch pre-order (plus de formulaire liste d’attente). */
export const JOURNEY_PRE_ORDER_COPY = {
  title: "YOUR NEXT JOURNEY STARTS HERE.",
  body: "Pre-order Original Bespoke — one 30 ml formula composed exclusively from your questionnaire. Ships within the week.",
  ctaLabel: PRE_ORDER_CTA_LABEL,
};
