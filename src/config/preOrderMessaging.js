import { BESPOKE_PRODUCT } from "./bespokeProduct";

/** Messages et route pre-order partagés (hero, sections, pages éditoriales). */
export const PRE_ORDER_PATH = BESPOKE_PRODUCT.path;

export const PRE_ORDER_STATUS = "— Limited commissions —";

export const PRE_ORDER_CTA_LABEL = "PRE-ORDER";
